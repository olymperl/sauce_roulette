export interface Student {
    // todo un student a un id, un name ainsi qu'une classe. Les 3 champs sont des chaînes de catractères
    id: string;
    name: string;
    classe: string;
}

export interface Classe {
    id: string;
    name: string;
}